@{
    ViewBag.Title = "Details";
}
@model NHiberanteDal.DTO.JournalModelDto[]
           

@if (Model.Count() > 0)
{ 
    foreach(var journal in Model)
    {
        @journal.Course.Name
        <ul>
            @foreach (var mark in journal.Marks)
            {
                    <li>@mark.Name - @mark.Value</li>
            }
        </ul>
    }
}

<h2>
    Details</h2>
