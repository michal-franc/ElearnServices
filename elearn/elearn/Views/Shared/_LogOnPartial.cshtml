@if(Request.IsAuthenticated) {
    <text>Welcome <b>@Html.ActionLink(Context.User.Identity.Name,"Index","Profile",null,null)</b>!
    [ @Html.ActionLink("Log Off", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Log On", "LogOn", "Account") ]
}
